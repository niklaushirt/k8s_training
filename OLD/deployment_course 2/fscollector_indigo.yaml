
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: indigo-collector-config
  namespace: default
data:
  COLLECTOR_CONFIG: 'http://indigo-student-ui'
  INSTRUCTOR_CONFIG: 'http://dashboard.default'
  APP_NAMESPACE: 'indigo'
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: indigo-student-ui
  namespace: default
  labels:
    app: indigo-student-ui
spec:
  selector:
    matchLabels:
      app: indigo-student-ui
  replicas: 1
  template:
    metadata:
      labels:
        app: indigo-student-ui
    spec:
      containers:
      - name: indigo-student-ui
        image: uk.icr.io/niklaushirt/collector:course0.3
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
          - name: APP_NAMESPACE
            valueFrom:
              configMapKeyRef:
                name: indigo-collector-config
                key: APP_NAMESPACE
          - name: APP_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: COLLECTOR_CONFIG
            valueFrom: 
              configMapKeyRef:
                name: indigo-collector-config
                key: COLLECTOR_CONFIG
        resources:
          requests:
            cpu: 80m
            memory: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  name: indigo-student-ui
  namespace: default
  labels:
    app: indigo-student-ui
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: indigo-student-ui
      nodePort: 31708
  selector:
    app: indigo-student-ui
